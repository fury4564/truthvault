{
    "name": "truthvault",
    "version": "1.0.0",
    "description": "Anonymous Secure Temporary Chat",
    "scripts": {
        "postinstall": "cd server && npm install && cd ../client && npm install",
        "build": "cd client && npm run build",
        "start": "cd server && node server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}